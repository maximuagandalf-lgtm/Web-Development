### Hooks and State
- Hooks are predefined functions, which makes it easier and efficient for us to develop dynamic webpages using react. 
Eg:- const [count, setCount] = useState(10)

- we can either use built-in hooks or develop our own hooks.
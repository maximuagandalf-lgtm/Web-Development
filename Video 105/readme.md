# Why React?
- We can use states whihc meand that once we update the varible, it changes accross the html page.

 - React is a huge library of JS code, which is written by someone else and we just use it to simplify out tasks.

 - "npx create-react-app 'filename' " this makes a react app. 

 - JSX is a HTMl like syntax but interpreted as JS by the compiler and is written in a JS file inside our react app.

 - Any variable which we can change and use in our DOM is called a state.

 - Components:- In JSX we can code different elements(cards, navbar) of our DOM as components and use them anywhere anytime we want.

 - Props:- In JSX props are much like parameters which are used to parse data. 

 - Debugging and maintainance in react is easy